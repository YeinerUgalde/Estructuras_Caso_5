#include <iostream>
#include "../socket/"
#include <vector>

class Users
{
    private:
        vector<Registered*> users;
    public:

        void init_users(){
            //se obtienen del conter y se le ponen al users
        }

        vector getUsers(){
            return users;
        }
}
